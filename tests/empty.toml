# Empty configuration file; used in tests to avoid interference from Black's own config.

# Explicitly disable _Nila_ and _Pyink_ modes so it's the same as the default _Black_ style.
[tool.nila]
pyink = false
nila = false
