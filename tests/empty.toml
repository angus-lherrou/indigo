# Empty configuration file; used in tests to avoid interference from Black's own config.

# Explicitly disable _Indigo_ and _Pyink_ modes so it's the same as the default _Black_ style.
[tool.indigo]
pyink = false
indigo = false
